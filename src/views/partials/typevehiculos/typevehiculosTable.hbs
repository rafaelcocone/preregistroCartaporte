{{#if typevehiculos}}
  <table class="table table-bordered table-hover table-dark">
    <thead>
      <tr>
        <th>Código</th>v
        <th>Tipo Vehiculo</th>
        <th> </th>
        <th> </th>

      </tr>
    </thead>
    <tbody>
      {{#each typevehiculos}}
        <tr>
          <td>{{code}}</td>
          <td>{{vehicleType}}</td>
          <td>
            <a href="/typevehiculos/{{id}}/edit" class="btn btn-info">
              Editar
            </a>
          </td>
          <td>
            <a href="/typevehiculos/{{id}}/delete" class="btn btn-danger">
              Eliminar
            </a>
          </td>
        </tr>

      {{/each}}
    </tbody>
  </table>

{{else}}
  <div class="h-100 d-flex align-item-center justify-content-center">
    <div class="text-center text-white">
      <h1>No hay tipo de vehiculo registrado</h1>
      <i class="far fa-clipboard fa-10x"></i>
    </div>

  </div>
{{/if}}